/**
 * Array = Think of it as   a variable
 * that can store miltple value
 */

let fruits = ["Apple", "Orange", "Banana"]

// fruits[0] = "Coconut"
fruits.push("Coconut") // add an last element\
fruits.pop() // remove last element
fruits.unshift("Mango") // added first element
fruits.shift() // remove first element

console.log(fruits);